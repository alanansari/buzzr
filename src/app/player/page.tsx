import PlayerConnection from "@/components/Player/PlayerSktConnection"

function Player() {

    return <>
        <PlayerConnection />
    </>
}

export default Player

// presenter screen -> show code
// screen 2 -> join room -> if already joined move to other screen
